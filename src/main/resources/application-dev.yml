spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-frosty-lab-aywdhpq4-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_lXoOuh7ZJtw6}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

logging:
  level:
    root: INFO
    com.familycareai: DEBUG
    org.springframework.security: INFO